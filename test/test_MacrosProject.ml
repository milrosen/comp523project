(* open MacrosProject.Reader
open Base *)

(* let test_reader test_name input expected =
    ( test_name
    , `Quick 
    , fun () ->  *)
        (* let actual = input |> read_str |> print_sexpr in
        let expected = expected |> print_sexpr in *)
        (* Alcotest.check Alcotest.string test_name expected actual) *)
(* 
let test_basic  =
    [test_reader "parses sucessfully" "(how (nested) (is (too) (nested)))"
        (List[Symbol "how"; List [Symbol "nested"] ; List [Symbol "is"; List [Symbol "too"]; List [Symbol "nested"]]])]

let () =
    Alcotest.run "tests" [("AST", test_basic)] ~compact:true *)